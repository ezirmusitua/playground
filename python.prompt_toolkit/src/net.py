def usenet(*args):
  if not args or len(args) == 0:
    print(
      'use command must have network parameter, for example: use net:mainnet'
    )
    return
  print('TODO: set current active network to *args[0]')
